//obj: pivot
//x min/max: range di movimento
function orizontalSwim(obj, xMin, xMax, zMin, zMax){

    var pos = obj.position; //Vector3

    if(pos.x > xMin && pos.x < xMax){
    //  var dir = 
    }
}
